# CDID 数据分析平台 - 待确认问题清单

**版本**: v0.2.0（基于 UI 确认）
**日期**: 2026-01-23
**目的**: 明确技术细节，确保方案可落地

---

## 📋 使用说明

请在每个问题下的 **【我的补充】** 部分填写答案。如果选项不适用，请直接描述实际情况。

---

## 第一部分：Gemini CLI 配置

### Q1: Gemini CLI 是否已安装？

**我的理解**：
- Gemini CLI 是生成报告的核心组件
- 需要通过 `npm install -g @google/generative-ai-cli` 安装
- 版本要求 >= v0.24.5

**选项**：
- A. 已安装（版本 >= v0.24.5）
- B. 已安装但版本较低（需要升级）
- C. 未安装（需要新安装）

**【我的补充】**：
```
你的答案：A

安装位置：
版本号：
```

---

### Q2: Gemini CLI 是否需要 API Key？

**我的理解**：
- 你提到 Gemini CLI 已经配置好账户，不需要 API Key
- 需要确认是通过什么方式认证的

**选项**：
- A. 不需要 API Key（已有内部配置）
- B. 需要 API Key，通过环境变量配置
- C. 需要 API Key，通过配置文件配置

**【我的补充】**：
```
你的答案：A

认证方式说明：已有内部配置

是否有调用限制（QPS/每日配额）：
```

---

### Q3: Gemini CLI 调用方式确认

**我的理解**：
- 报告生成采用 stdin 输入分析结果（JSON）+ prompt 触发能力（`@skill_name`）
- 注意：现网 Gemini CLI 的 `-s` 用于 sandbox，不用于选择 Skill

**选项**：
- A. 支持管道输入 + `-s` 参数指定 Skill
- B. 不支持 `-s` 参数，需要用其他方式
- C. 其他调用方式（请说明）

**【我的补充】**：
```
你的答案：B 

实际调用命令示例：提示中使用 @skill_name 语法来调用，如 gemini -p "@github 列出我的开放PR"，

是否有调用超时限制：暂不考虑
单次调用的 token 限制：暂不考虑
```

---

## 第二部分：内网 API 配置

### Q4: 内网 API 是否需要认证？

**我的理解**：
- 目标 API：`http://172.17.129.204:6829`
- 需要明确是否需要 Token/签名

**选项**：
- A. 不需要认证（内网信任）
- B. 需要 Token 认证（在 Header 中传递）
- C. 需要签名认证（请求参数签名）

**【我的补充】**：
```
你的答案：A

如果需要认证，请提供：
认证方式（Bearer Token / API Key / 签名算法）：
如何获取 Token：
Token 有效期：
```

---

### Q5: 内网 API 是否有调用频率限制？

**我的理解**：
- 需要明确 QPS 限制、并发限制
- 影响 Worker 的轮询策略

**选项**：
- A. 无限制（内网随意调用）
- B. 有限制（请在补充中说明具体数值）
- C. 不清楚（需要测试）

**【我的补充】**：
```
你的答案：A

QPS 限制（每秒请求数）：
单个 IP 并发限制：
是否有每日调用上限：
```

---

### Q6: 创建任务接口的 start/end 参数是否必填？

**我的理解**：
- 原文档中参数表标注 `start/end` 为必填
- 但 curl 示例中未包含这两个参数
- 需要明确实际要求

**选项**：
- A. 必填
- B. 非必填（有默认值）
- C. 其他情况（请说明）

**【我的补充】**：
```
你的答案：必填

**参数**
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| name | string | 是 | 任务名称 |
| package_name | string | 是 | 包名 |
| file | file | 是 | 包含 CDID 列表的 csv 文件 |
| start | string | 是 | 开始日期 YYYY-MM-DD |
| end | string | 是 | 结束日期 YYYY-MM-DD |
| message | string | 否 | 数据类型 dna/daa，可多选 |

如果必填，格式要求（YYYY-MM-DD？）：
如果非必填，默认值是什么：
时间范围的业务含义（查询哪个时间段的数据）：
```

---

### Q7: 内网 API 的平均处理时长是多少？

**我的理解**：
- 影响轮询间隔和超时设置
- 需要明确典型耗时和最长耗时

**选项**：
- A. 很快（< 1 分钟）
- B. 较快（1-5 分钟）
- C. 较慢（5-30 分钟）
- D. 很慢（> 30 分钟）

**【我的补充】**：
```
你的答案：C

典型处理时长（分钟）：
最长处理时长（分钟）：
是否有超时限制：
```

---

## 第三部分：数据分析逻辑

### Q8: "多发检测" 的具体定义是什么？

**我的理解**：
- 检测多个 CDID 共享相同的设备指纹（did/oaid/imei）
- 需要明确具体算法规则

**选项**：
- A. 按设备指纹（did/oaid/imei）分组，cdid 数量 > 1 即为多发
- B. 需要满足更复杂的条件（请在补充中说明）
- C. 不清楚原算法，需要提供原代码或文档

**【我的补充】**：
```
你的答案：A

多发的判定条件（详细描述）：
有多种判定 如 did去重/OAID去重 >1 即为多发, did去重/安卓id去重 >1 即为多发

是否有阈值（例如：cdid 数量 > N 才算多发）：测试仅需要实现最简单功能

输出格式要求（需要哪些字段）：
```

---

### Q9: "碰撞检测" 的具体定义是什么？

**我的理解**：
- 检测同一个设备标识（did/oaid/imei）关联多个不同的 cdid
- 与多发检测类似但侧重点不同？

**选项**：
- A. 检测同一设备 ID 对应多个 cdid（与多发相反的视角）
- B. 检测不同设备 ID 之间的关联关系
- C. 其他定义（请说明）

**【我的补充】**：
```
你的答案：C

碰撞的判定条件（详细描述）：相同 DID 不同 设备型号


关键字段（did? oaid? imei?）：

输出格式要求：
```

---

### Q10: "风险检测" 的具体定义是什么？

**我的理解**：
- 检测 `rc_rules` 和 `rc_scenes` 字段
- 某些规则版本或场景标识代表高风险

**选项**：
- A. 简单规则：rc_rules 或 rc_scenes 不为空即为风险
- B. 复杂规则：需要匹配特定的规则版本或场景（请提供规则列表）
- C. 其他定义（请说明）

**【我的补充】**：
```
你的答案：C 和风险表中的异常一致

风险判定条件：


高风险的 rc_rules 值（枚举/正则）：

高风险的 rc_scenes 值：

风险等级划分（是否有高/中/低风险）：
```

---

### Q11: 是否有测试数据？

**我的理解**：
- 开发阶段需要测试数据验证分析逻辑
- 理想情况：有真实数据样例（脱敏后）

**选项**：
- A. 有测试数据（请提供文件路径或样例）
- B. 可以提供测试数据
- C. 没有测试数据（需要自己构造）

**【我的补充】**：
```
你的答案：A

测试数据位置：data_fenxi\xuqiu\demo\波克问题数据.csv

数据规模（行数）：小于 50行

是否包含已知的多发/碰撞/风险案例：
```

---

## 第四部分：管线配置

### Q12: Python 脚本的命名规范是什么？

**我的理解**：
- 你提到脚本命名格式：用户信息+时间信息+分析编号
- 需要明确具体规范

**选项**：
- A. 固定格式：`{user_id}_{timestamp}_{number}.py`
- B. 固定脚本名：`analyzer.py`，通过目录区分
- C. 其他规范（请说明）

**【我的补充】**：
```
你的答案：B

脚本命名示例：

脚本目录结构示例：
```

---

### Q13: Python 脚本如何被 Worker 调用？

**我的理解**：
- 方案采用标准化接口：`def analyze(input_file, user_info, start_date, end_date) -> dict`
- Worker 通过动态导入调用

**确认**：
- ✅ 这个方案是否可行？
- ✅ 脚本是否需要依赖特定的环境或库？

**【我的补充】**：
```
你的答案：

是否可行：是

脚本依赖的库（除 Pandas/NumPy 外）：暂时没有

脚本是否需要访问外部资源（数据库/API）：不需要
```

---

### Q14: Gemini Skill 的目录结构是什么？

**我的理解**：
- Skill 是一个包含配置文件的目录
- 不同 Skill 的目录结构不同，所以不能在 UI 中新建

**需要确认**：
- Skill 目录的典型结构？
- 必须包含哪些文件？

他是再Gemini cli目录下 ./skills/目录下 每一个skill一个目录。这个原则上你不需要处理，添加skill采用导入方式，人工处理位置，和程序无关。你只需要调用就行了 调用的方法是读取 文件中的'name: insight'直接传入的prompt就是 使用 insight 分析 这里漏掉了，就是输出文件的目录需要设定再指定目录下，再这里才能用prompt，把目录也传进去

**【我的补充】**：
```
你的答案：

Skill 目录结构示例：
skill_name/
├── SKILL.md (必须？)
├── prompt.txt (必须？)
└── ...

必须包含的文件：

Skill 配置文件格式：
```

---

### Q15: Skill 导入后是否需要重启 Gemini CLI？

**我的理解**：
- 导入 Skill 后，需要重启 Gemini CLI 才能识别新 Skill

**选项**：
- A. 需要重启 Gemini CLI
- B. 不需要重启，自动识别
- C. 需要执行特定命令刷新

**【我的补充】**：
```
你的答案：A

如何重启 Gemini CLI：手动重启

是否有命令可以列出所有可用 Skill：
```

---

## 第五部分：客户配置

### Q16: 客户-包名映射是否已有现成数据？

**我的理解**：
- 需要导入客户-包名映射数据
- 如果有现成数据，可以批量导入

**选项**：
- A. 有现成数据（Excel/CSV）
- B. 没有现成数据，需要手动录入
- C. 可以从其他系统导出

**【我的补充】**：
```
你的答案：A

数据位置：

数据格式（Excel/CSV）：

数据量（大约多少个客户）：500
```

---

### Q17: 包名是否有可能一对多？

**我的理解**：
- 一个客户对应一个包名
- 需要确认是否存在一个客户有多个包名的情况

**选项**：
- A. 一对一（一个客户一个包名）
- B. 一对多（一个客户多个包名）
- C. 多对一（多个客户共享一个包名）

**【我的补充】**：
```
你的答案：A

如果是一对多，如何处理：

是否需要在 UI 中支持多选包名：
```

---

## 第六部分：报告格式要求

### Q18: 报告是否有固定模板或示例？

**我的理解**：
- Gemini Skill 会根据 prompt 生成报告
- 如果有示例，可以更好地设计 Skill

**选项**：
- A. 有固定模板（请提供模板文件或截图）
- B. 有示例报告（请提供）
- C. 没有固定要求（由 Gemini 自由发挥）

**【我的补充】**：
```
你的答案：C

模板/示例位置：

必须包含的章节：
1.
2.
3.

报告语言（中文？英文？）：

报告语气（正式？通俗？）：
```

---

### Q19: 报告是否需要可视化（图表）？

**我的理解**：
- HTML 报告可以内嵌 Chart.js 或其他图表库
- 需要明确是否需要图表、需要哪些类型

**选项**：
- A. 需要图表（饼图、柱状图、趋势图等）
- B. 不需要图表（纯文字 + 表格即可）
- C. 可选（Gemini 自行决定）

**【我的补充】**：
```
你的答案：C

需要的图表类型：
- [ ] 饼图（例如：风险占比）
- [ ] 柱状图（例如：检测类型统计）
- [ ] 趋势图（例如：时序变化）
- [ ] 其他：

图表库偏好（Chart.js? ECharts? 其他？）：
```

---

## 第七部分：性能与规模

### Q20: 单次任务的典型数据量是多少？

**我的理解**：
- 影响性能优化策略（内存、并发、超时设置）
- 需要明确典型规模和极端情况

**选项**：
- A. 小规模（10-100 条 CDID）
- B. 中规模（100-1000 条 CDID）
- C. 大规模（1000+ 条 CDID）

**【我的补充】**：
```
你的答案：B

典型 CDID 数量：

最大 CDID 数量（极端情况）：

单条记录的字段数（Excel 列数）：

预估文件大小（MB）：
```

---

### Q21: 预期同时运行多少个任务？

**我的理解**：
- MVP 阶段单线程 FIFO（一次一个）
- 但需要了解未来并发需求

**选项**：
- A. 单任务（MVP 足够）
- B. 少量并发（2-5 个）
- C. 大量并发（10+ 个）

**【我的补充】**：
```
你的答案：A

MVP 阶段并发数：

未来预期并发数：

是否需要任务优先级（VIP 任务优先）：
```

---

## 第八部分：用户与权限

### Q22: 用户权限接口是什么？

**我的理解**：
- 用户权限由现有接口控制
- 需要明确接口的调用方式

**需要提供**：
- 权限接口的 URL
- 调用方式（HTTP API？RPC？）
- 接口文档

**【我的补充】**：
```
你的答案：A

权限接口 URL：前期先屏蔽掉，后期调试的时候再说

调用方式（GET/POST/其他）：

接口文档位置：

返回格式示例：
```

---

### Q23: 是否需要审计日志？

**我的理解**：
- 记录用户的操作（创建任务、删除任务等）
- 便于追溯和审计

**选项**：
- A. MVP 必须支持
- B. 未来需要支持
- C. 不需要

**【我的补充】**：
```
你的答案：B

需要记录哪些操作：

日志保留时长：
```

---

## 第九部分：部署与运维

### Q24: 部署环境是什么？

**我的理解**：
- 需要明确部署环境（Docker、裸机、K8s 等）
- 影响部署方案设计

**选项**：
- A. Docker（推荐）
- B. 裸机部署（Linux）
- C. Kubernetes
- D. 其他

**【我的补充】**：
```
你的答案：A

部署环境：

操作系统版本：

是否有 CI/CD 流程：
```

---

### Q25: 是否需要监控告警？

**我的理解**：
- 监控系统状态（Worker 健康、任务失败率等）
- 失败时发送告警

**选项**：
- A. MVP 必须支持
- B. 未来需要支持
- C. 不需要

**【我的补充】**：
```
你的答案：B

监控指标需求：
- [ ] Worker 健康检查
- [ ] 任务成功率
- [ ] API 响应时间
- [ ] 其他：

告警方式（邮件？钉钉？企业微信？）：
```

---

## 📝 填写完成后

请将填写好的文档发送给开发团队，或直接在对话中回复关键问题的答案。

**优先级最高的问题**（必须先回答）：
- Q1-Q3（Gemini CLI 配置）
- Q4-Q7（内网 API）
- Q8-Q10（数据分析算法）
- Q12-Q15（管线配置）

**可以后续补充的问题**：
- Q11（测试数据）
- Q16-Q17（客户配置）
- Q18-Q19（报告格式）
- Q20-Q23（性能与规模、权限）
- Q24-Q25（部署与运维）

---

## 📊 问题统计

- **总问题数**：25 个
- **核心问题**：10 个（Q1-Q7, Q12-Q15）
- **次要问题**：15 个（其他）

---

**感谢你的配合！** 🙏
