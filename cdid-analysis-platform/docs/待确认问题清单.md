# CDID 数据分析平台 - 待确认问题清单

**版本**: v0.1.0
**日期**: 2026-01-23
**目的**: 明确技术细节，确保方案可落地

---

## 📋 使用说明

请在每个问题下的 **【我的补充】** 部分填写答案。如果选项不适用，请直接描述实际情况。

---

## 第一部分：Gemini CLI 配置

### Q1: Gemini CLI 是否已安装？

**我的理解**：
- Gemini CLI 是生成报告的核心组件
- 需要通过 `npm install -g @google/generative-ai-cli` 安装
- 版本要求 >= v0.24.5

**选项**：
- A. 已安装（版本 >= v0.24.5）
- B. 已安装但版本较低（需要升级）
- C. 未安装（需要新安装）

**【我的补充】**：
```
你的答案：A

安装位置：
版本号：
```

---

### Q2: Gemini CLI 是否需要 API Key？

**我的理解**：
- Gemini CLI 可能需要 Google API Key 才能调用 Gemini 模型
- 需要明确如何配置（环境变量？配置文件？）

**选项**：
- A. 需要 API Key，通过环境变量 `GEMINI_API_KEY` 配置
- B. 需要 API Key，通过配置文件配置
- C. 不需要 API Key（已有内部配置）

**【我的补充】**：
```
你的答案：C

API Key 获取方式：
配置方式：
是否有调用限制（QPS/每日配额）：
```

---

### Q3: Gemini CLI Headless Mode 调用方式确认

**我的理解**：
- 计划通过管道输入分析结果，让 Gemini CLI 生成 HTML 报告
- 命令示例：`cat analysis_result.json | gemini -p "生成报告..."`

**选项**：
- A. 支持管道输入（stdin），按上述方式调用
- B. 不支持管道，需要通过文件输入（`gemini -p "..." < input.json`）
- C. 需要其他调用方式（请在补充中说明）

**【我的补充】**：
```
你的答案：你需要通过网络查找，找到正确的指令，没有使用限制

实际调用命令示例：

是否有调用超时限制：
单次调用的 token 限制：
```

---

## 第二部分：内网 API 配置

### Q4: 内网 API 是否需要认证？

**我的理解**：
- 目标 API：`http://172.17.129.204:6829`
- xuqiu/gemini/insight/SKILL.md 中的 curl 示例未包含认证头
- 需要明确是否需要 Token/签名

**选项**：
- A. 不需要认证（内网信任）
- B. 需要 Token 认证（在 Header 中传递）
- C. 需要签名认证（请求参数签名）

**【我的补充】**：
```
你的答案：A

如果需要认证，请提供：
认证方式（Bearer Token / API Key / 签名算法）：
如何获取 Token：
Token 有效期：
```

---

### Q5: 内网 API 是否有调用频率限制？

**我的理解**：
- 需要明确 QPS 限制、并发限制
- 影响 Worker 的轮询策略（轮询间隔、超时时间）

**选项**：
- A. 无限制（内网随意调用）
- B. 有限制（请在补充中说明具体数值）
- C. 不清楚（需要测试）

**【我的补充】**：
```
你的答案：A

QPS 限制（每秒请求数）：
单个 IP 并发限制：
是否有每日调用上限：
```

---

### Q6: 内网 API 是否有测试环境？

**我的理解**：
- 开发阶段需要测试环境，避免污染生产数据
- 理想情况：测试环境 + 生产环境分离

**选项**：
- A. 有独立测试环境（提供地址）
- B. 没有测试环境（直接用生产环境）
- C. 可以搭建测试环境

**【我的补充】**：
```
你的答案：B

测试环境地址（如有）：
测试数据获取方式：
```

---

### Q7: 创建任务接口的 start/end 参数是否必填？

**我的理解**：
- xuqiu/gemini/insight/SKILL.md 中参数表标注 `start/end` 为必填
- 但 curl 示例中未包含这两个参数
- 需要明确实际要求

**选项**：
- A. 必填（文档错误，curl 示例需要补充）
- B. 非必填（参数表错误，有默认值）
- C. 其他情况（请说明）

**【我的补充】**：
```
你的答案：C
参看 xuqiu/gemini/insight/SKILL.md文件

如果必填，格式要求（YYYY-MM-DD？）：
如果非必填，默认值是什么：
时间范围的业务含义（查询哪个时间段的数据）：
```

---

## 第三部分：数据分析逻辑

### Q8: "多发检测" 的具体定义是什么？

**我的理解**：
- 原 analyzer 二进制实现了多发检测（duplicate）
- 猜测：检测多个 CDID 共享相同的设备指纹（did/oaid/imei）
- 需要明确具体算法规则

**选项**：
- A. 按设备指纹（did/oaid/imei）分组，cdid 数量 > 1 即为多发
- B. 需要满足更复杂的条件（请在补充中说明）
- C. 不清楚原算法，需要提供原代码或文档

**【我的补充】**：
```
你的答案：C 

多发的判定条件（详细描述）：（cdid去重 / oaid去重）>2 


是否有阈值（例如：cdid 数量 > N 才算多发）：

输出格式要求（需要哪些字段）：
```

---

### Q9: "碰撞检测" 的具体定义是什么？

**我的理解**：
- 猜测：检测同一个设备标识（did/oaid/imei）关联多个不同的 cdid
- 与多发检测类似但侧重点不同？

**选项**：
- A. 检测同一设备 ID 对应多个 cdid（与多发相反的视角）
- B. 检测不同设备 ID 之间的关联关系
- C. 其他定义（请说明）

**【我的补充】**：
```
你的答案：咱不需要实现

碰撞的判定条件（详细描述）：


关键字段（did? oaid? imei?）：

输出格式要求：
```

---

### Q10: "风险检测" 的具体定义是什么？

**我的理解**：
- 检测 `rc_rules` 和 `rc_scenes` 字段
- 猜测：某些规则版本或场景标识代表高风险

**选项**：
- A. 简单规则：rc_rules 或 rc_scenes 不为空即为风险
- B. 复杂规则：需要匹配特定的规则版本或场景（请提供规则列表）
- C. 其他定义（请说明）

**【我的补充】**：
```
你的答案：咱不需要实现

风险判定条件：


高风险的 rc_rules 值（枚举/正则）：

高风险的 rc_scenes 值：

风险等级划分（是否有高/中/低风险）：
```

---

### Q11: 是否有测试数据？

**我的理解**：
- 开发阶段需要测试数据验证分析逻辑
- 理想情况：有真实数据样例（脱敏后）

**选项**：
- A. 有测试数据（请提供文件路径或样例）
- B. 可以提供测试数据
- C. 没有测试数据（需要自己构造）

**【我的补充】**：
```
你的答案：A

测试数据位置：xuqiu/demo/波克问题数据.csv

数据规模（行数）：

是否包含已知的多发/碰撞/风险案例：
```

---

## 第四部分：报告格式要求

### Q12: 报告是否有固定模板或示例？

**我的理解**：
- Gemini CLI 会根据 prompt 生成报告
- 如果有示例，可以更好地设计 prompt

**选项**：
- A. 有固定模板（请提供模板文件或截图）
- B. 有示例报告（请提供）
- C. 没有固定要求（由 Gemini 自由发挥）

**【我的补充】**：
```
你的答案：C

模板/示例位置：

必须包含的章节：
1.
2.
3.

报告语言（中文？英文？）：

报告语气（正式？通俗？）：
```

---

### Q13: 报告必须包含哪些内容？

**我的理解**：
- 猜测：执行摘要、多发检测结果、碰撞检测结果、风险检测结果、建议

**选项**：
- A. 按上述猜测即可
- B. 有额外要求（请在补充中列出）
- C. 灵活处理（无硬性要求）

**【我的补充】**：
```
你的答案：C

必须包含的章节（优先级排序）：
1.
2.
3.

可选章节：

不需要的内容：
```

---

### Q14: 报告是否需要可视化（图表）？

**我的理解**：
- HTML 报告可以内嵌 Chart.js 或其他图表库
- 需要明确是否需要图表、需要哪些类型

**选项**：
- A. 需要图表（饼图、柱状图、趋势图等）
- B. 不需要图表（纯文字 + 表格即可）
- C. 可选（Gemini 自行决定）

**【我的补充】**：
```
你的答案：C

需要的图表类型：
- [ ] 饼图（例如：风险占比）
- [ ] 柱状图（例如：检测类型统计）
- [ ] 趋势图（例如：时序变化）
- [ ] 其他：

图表库偏好（Chart.js? ECharts? 其他？）：
```

---

## 第五部分：性能与规模

### Q15: 单次任务的典型数据量是多少？

**我的理解**：
- 影响性能优化策略（内存、并发、超时设置）
- 需要明确典型规模和极端情况

**选项**：
- A. 小规模（10-100 条 CDID）
- B. 中规模（100-1000 条 CDID）
- C. 大规模（1000+ 条 CDID）

**【我的补充】**：
```
你的答案：B

典型 CDID 数量：

最大 CDID 数量（极端情况）：

单条记录的字段数（Excel 列数）：

预估文件大小（MB）：
```

---

### Q16: 内网 API 的平均处理时长是多少？

**我的理解**：
- 影响轮询间隔和超时设置
- 需要明确典型耗时和最长耗时

**选项**：
- A. 很快（< 1 分钟）
- B. 较快（1-5 分钟）
- C. 较慢（5-30 分钟）
- D. 很慢（> 30 分钟）

**【我的补充】**：
```
你的答案：C

典型处理时长（分钟）：

最长处理时长（分钟）：

是否有超时限制：
```

---

### Q17: 预期同时运行多少个任务？

**我的理解**：
- MVP 阶段单线程 FIFO（一次一个）
- 但需要了解未来并发需求

**选项**：
- A. 单任务（MVP 足够）
- B. 少量并发（2-5 个）
- C. 大量并发（10+ 个）

**【我的补充】**：
```
你的答案：B

MVP 阶段并发数：

未来预期并发数：

是否需要任务优先级（VIP 任务优先）：
```

---

### Q18: 是否需要支持批量任务？

**我的理解**：
- 批量任务：一次性创建多个任务（例如：上传多个文件）
- MVP 阶段可以不支持，但需要明确未来需求

**选项**：
- A. MVP 必须支持
- B. 未来需要支持
- C. 不需要支持

**【我的补充】**：
```
你的答案：B

批量任务的使用场景：

单次批量的任务数量：
```

---

## 第六部分：其他需求

### Q19: 是否需要用户权限控制？

**我的理解**：
- MVP 阶段可能不需要登录/鉴权（内网环境）
- 但需要明确未来是否需要

**选项**：
- A. MVP 不需要（内网信任）
- B. MVP 需要（简单的用户名密码）
- C. 未来需要（LDAP/OAuth2）

**【我的补充】**：
```
你的答案：A

MVP 阶段：

未来需求：

是否需要审计日志（记录谁创建了任务）：
```

---

### Q20: 是否需要导出功能（报告导出为 PDF/Excel）？

**我的理解**：
- HTML 报告可以在浏览器中打开
- 是否需要额外的导出功能

**选项**：
- A. MVP 必须支持
- B. 未来需要支持
- C. 不需要（HTML 足够）

**【我的补充】**：
```
你的答案：B

需要导出的格式：
- [ ] PDF
- [ ] Excel
- [ ] Word
- [ ] 其他：

导出的触发方式（页面按钮？自动生成？）：
```

---

## 📝 填写完成后

请将填写好的文档发送给开发团队，或直接在对话中回复关键问题的答案。

**优先级最高的问题**（必须先回答）：
- Q1-Q3（Gemini CLI 配置）
- Q4-Q7（内网 API）
- Q8-Q10（数据分析算法）

**可以后续补充的问题**：
- Q11-Q14（报告格式）
- Q15-Q18（性能规模）
- Q19-Q20（其他需求）

---

**感谢你的配合！** 🙏
